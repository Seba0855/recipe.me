# Build audit — recipe.me

generated: 2026-07-28T21:27:47+02:00

## Identity

| item | value |
|---|---|
| path | /c/Users/Sebastian/AndroidStudioProjects/recipe.me |
| commit | 0b4c591 |
| branch | develop |
| dirty | YES — audit reflects uncommitted state |
| Gradle (wrapper) | 8.0.2 |
| AGP | ? |
| Kotlin | 1.8.22 |
| compileSdk | ? |
| minSdk | ? |

## Scale [1.4.1, 2.2]

Modules declared in `settings.gradle.kts`: **25**

Lines of Kotlin/Java per module directory (top 15):
```
    1110  ./ui-home
     847  ./ui-scanner
     633  ./ui-common
     446  ./domain-recipes
     348  ./datasource-recipes-network
     259  ./ui-saved
     238  ./domain-common
     206  ./build-logic/convention
     187  ./model-recipes
     184  ./database
     157  ./domain-products
     134  ./retrofit-base
     134  ./app
     129  ./barcode-scanner-local
     120  ./retrofit-deepl
```
> A single module holding >40% of total LOC is a monolith wearing a multi-module
> costume — flag it for 3.2.2.

## Variant explosion [1.2.5, 1.3.1, 1.4.3 -> 3.1]

Flavor dimensions found:
```
(none)
```
Product flavors (name + dimension):
```
(none — flavors not used)
```
Build types:
```
(defaults only: debug, release)
```

**Compute the variant count as the product of (flavors per dimension) x (build types)**
and record the arithmetic explicitly. This is the number that settles the unverified
"160 000 configurations" figure — do not carry that figure into chapter 3 unverified.

Cross-check with:
```
./gradlew :app:tasks --all | grep -cE '^assemble[A-Z]'
```

## Configuration surface [1.3.3, 1.3.4]

```properties
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true
org.gradle.caching=true
org.gradle.parallel=true
org.gradle.daemon=true
org.gradle.unsafe.configuration-cache=true
```

Flags to look for and what their absence means:

| flag | present? | thesis |
|---|---|---|
| `org.gradle.caching` | true | 1.3.3 |
| `org.gradle.configuration-cache` | **absent** | 1.3.3 |
| `org.gradle.parallel` | true | 1.3.4 |
| `org.gradle.workers.max` | **absent** | 1.3.4 |
| `org.gradle.isolated-projects` | **absent** | 1.3.3 |
| `org.gradle.jvmargs` | -Xmx2048m -Dfile.encoding=UTF-8 | 1.3.4 |
| `kotlin.daemon.jvmargs` | **absent** | 1.3.4 |
| `android.nonTransitiveRClass` | true | 1.3.4 |
| `android.enableJetifier` | **absent** | 1.5.3 |
| `android.enableR8.fullMode` | **absent** | 1.2.4 |

> Absent != off. Check the current default for the Gradle/AGP version in use before
> writing a sentence about it.

## Dependency wiring [1.4.2]

`api(project(...))`: **0**   |   `implementation(project(...))`: **0**

Every `api(project(...))` below needs a justification — a type from that dependency
must actually appear in the module's public signature (return type, parameter, or
supertype). Otherwise it is an invalidation channel with no benefit [1.4.2].
```
(none)
```

## Reverse dependencies — blast radius [1.4.2, 1.4.4]

How many modules depend on each module (descending). The top entry is the
god-module candidate; multiply by its change frequency for severity.
```
   9  :datasource-common
   6  :domain-common
   5  :model-products
   4  :ui-common
   4  :retrofit-base
   4  :model-recipes
   4  :model-deepl
   4  :datasource-products
   3  :domain-recipes
   3  :datasource-translation
   3  :datasource-recipes
   2  :retrofit-openfoodfacts
   2  :retrofit-deepl
   2  :database
   2  :barcode-scanner
```
Change frequency of the top candidates (commits touching them, last 12 months):
```
   0  :ui-scanner
   0  :ui-saved
   0  :ui-home
   0  :ui-common
   0  :retrofit-recipes
   0  :retrofit-openfoodfacts
   0  :retrofit-deepl
   0  :retrofit-base
   0  :model-recipes
   0  :model-products
```
> severity = reverse-dependency count x change frequency. A widely-depended-on
> module that never changes is harmless [1.4.4].

## Build logic [1.5.1, 1.5.2]

| item | value |
|---|---|
| `buildSrc/` | absent |
| `includeBuild` |  includeBuild("build-logic")  |
| build-logic subprojects | 1 |
| version catalogs | libs.versions.toml |
| legacy `ext.` versions | 0
0 |
| `Dependencies.kt` | absent |

> A single undivided `build-logic` module is not an improvement over `buildSrc` in
> invalidation scope — only granular subprojects deliver that [1.5.2].

## Hidden rebuild sources [1.5.3]

BuildConfig enabled in:
```
./retrofit-base/build.gradle.kts:21:        buildConfig = true
./retrofit-deepl/build.gradle.kts:21:        buildConfig = true
./retrofit-openfoodfacts/build.gradle.kts:20:        buildConfig = true
```
BuildConfig fields derived from git or time — these change every commit and
invalidate the module's compilation on every commit:
```
(none)
```
Work executed during the configuration phase [1.3.1]:
```
(none found)
```
Code-generating plugins (each adds a task to the critical path):
```
alias(libs.plugins.android.application)
alias(libs.plugins.android.library)
alias(libs.plugins.dagger.hilt)
alias(libs.plugins.kotlin.android)
alias(libs.plugins.kotlin.jvm)
alias(libs.plugins.kotlin.parcelize)
alias(libs.plugins.kotlin.serialization)
alias(libs.plugins.ksp)
alias(libs.plugins.ktlint)
alias(libs.plugins.navigation.safeargs)
alias(libs.plugins.secrets.plugin)
```

## Annotation processing [1.2.2 -> 3.2.3]

Modules using **kapt**: 0   |   modules using **KSP**: 18


## Shrinking [1.2.4]

```
(none)
```
> Minification enabled in a debug-like build type is a direct, cheap finding for 3.2.

## Dependency determinism [1.5.4]

Dynamic versions:
```
(none — good)
```
| item | value |
|---|---|
| `gradle.lockfile` | 0 file(s) |
| dependency verification | absent |

## CI configuration [3.4.2]

```
.github/workflows/android.yml
(no CI config found)
```
Gradle invocations in CI (check for bare `assemble`, which builds every variant):
```
26:#        run: chmod +x gradlew
60:#        run: ./gradlew assembleDebug --scan
64:#        run: ./gradlew ktlintCheck
(none)
```

## Next steps

1. Fill in the variant arithmetic explicitly (flavors per dimension x build types).
2. Pick ABI/non-ABI target files: one leaf module, one mid-graph, one god-module.
   Record why each was chosen — target choice must follow from the reverse-dependency
   table above, not from convenience.
3. Run `verify-container-limits.sh` before measuring anything.
4. Only then run `run-benchmark.sh`.

Not measured / limitations: record here everything deliberately skipped and why.
Section 2.6 is assembled from this list, and a threats-to-validity section written from
memory at the end is always weaker than one accumulated as the work happens.

---

# Addendum — manual verification, 2026-07-28

The generated section above is the raw output of `audit-project.sh`. Several of its
fields are wrong or empty because of environment limits (`bc` is absent from Git Bash on
this machine, and some greps assume the modern AGP DSL). Everything below was verified by
reading the files directly. **Where the two disagree, this addendum is authoritative.**

## Corrections to the generated section

| generated field | generated value | verified value | why the script was wrong |
|---|---|---|---|
| `dirty` | `YES` | **no** at tag time | the audit output file itself already existed in the worktree when the script ran; `git status --porcelain` was empty immediately before `thesis-baseline` was tagged |
| AGP | `?` | **8.1.1** (`android-gradle` in `libs.versions.toml`) | script greps keys `agp`/`androidGradlePlugin`/`android-gradle-plugin`; this catalog uses `android-gradle` |
| compileSdk | `?` | **`android-34`** | set in the convention plugin (Kotlin), not in any `*.gradle.kts` |
| minSdk | `?` | **26** | same |
| `implementation(project(...))` | `0` | **66** | `bc` not installed — the arithmetic pipeline collapsed to 0 |
| `api(project(...))` | `0` | **0** (genuinely zero) | coincidentally correct |
| build types | "(defaults only)" | **correct** — `debug`, `release`, both via `getByName` in the convention plugin | — |
| CI config | listed `android.yml` then "(no CI config found)" | file exists but is **100 % commented out** | script printed both branches |
| `legacy ext. versions` | `0` newline `0` | 0 | formatting artefact |

## Identity (verified)

| item | value |
|---|---|
| commit | `0b4c591cc688042badb5e21d353171b887a8daaa`, tag `thesis-baseline` |
| branch | `develop` |
| Gradle wrapper | 8.0.2 |
| AGP (applied) | 8.1.1 |
| AGP (compiled against in `build-logic`) | **7.3.1** — `gradlePlugin = "7.3.1"` feeds `android-gradlePlugin`, which `build-logic/convention` consumes as `compileOnly` |
| Kotlin | 1.8.22 (K1; catalog also carries `kotlinSerialization = 1.8.20`) |
| KSP | 1.9.0-1.0.12 — **built for Kotlin 1.9.0, applied to a 1.8.22 build** |
| JDK / toolchain | `jvmToolchain(17)`; launcher JDK 17.0.18 (Corretto) |
| modules | 25 in `settings.gradle.kts`, plus `build-logic:convention` |
| total Kotlin/Java LOC | 5 681 |
| largest module | `:ui-home`, 1 110 LOC = 19.5 % of total — **no monolith**; `:app` is 134 LOC |

## Variant count (settles the "160 000 configurations" note)

- `flavorDimensions` declared anywhere: **0**. `grep -rniE 'flavorDimension|productFlavors|\bflavor\b'` over every `*.gradle.kts` and every convention-plugin `*.kt` returns nothing.
- product flavors: **0**
- build types: **2** — `debug` and `release`, both AGP defaults, reached with `getByName` in `AndroidApplicationPlugin.kt:42-58`; no `create(` or `register(` of any additional build type anywhere in the project.

Arithmetic: with no flavor dimensions the product over dimensions is the empty product, 1.

**1 × 2 = 2 variants** (`debug`, `release`).

Cross-check `./gradlew :app:tasks --all | grep -cE '^assemble[A-Z]'` **was not run**. Every
Gradle invocation in this environment fails at daemon start with
`java.io.IOException: Unable to establish loopback connection` — reproduced with the daemon,
with `--no-daemon`, and with the tool sandbox disabled. `./gradlew --version` works, because
it needs no daemon. The figure of 2 rests on the static evidence listed above, which is
exhaustive for this project, but the cross-check must still be run on the author's own shell
before the number is quoted in 3.1. See "Not measured".

For contrast, Signal is 4 × 2 × 6 = 48 variants. Variant explosion is therefore a **scale
phenomenon demonstrable only on Signal**, and merely latent here. That is a legitimate
division of labour between the two projects, not a hole in the study.

## Dependency graph (verified)

`api(project(...))`: **0**   `implementation(project(...))`: **66**

Every inter-module edge is `implementation`. Compile avoidance is therefore already maximal
at the wiring level — an ABI change propagates exactly one hop and no further. There is
**no `api`→`implementation` intervention available on this project**.

Reverse dependencies (direct consumers) and change frequency (full history: 171 commits,
2023-03-19 → 2024-05-01). Severity = consumers × commits.

| module | consumers | commits | severity |
|---|---|---|---|
| `:ui-common` | 4 | 31 | **124** |
| `:domain-common` | 6 | 14 | **84** |
| `:domain-recipes` | 3 | 21 | 63 |
| `:ui-home` | 1 | 40 | 40 |
| `:model-recipes` | 4 | 8 | 32 |
| `:model-products` | 5 | 5 | 25 |
| `:retrofit-base` | 4 | 6 | 24 |
| `:datasource-common` | **9 (max)** | 2 | 18 |
| `:datasource-recipes` | 3 | 5 | 15 |
| `:datasource-products` | 4 | 2 | 8 |

`:datasource-common` has the largest blast radius but almost never changes; `:ui-common` has
the highest severity. The audit-matrix metric "reverse-deps × change frequency" separates
those two cleanly here, which makes this table a good figure candidate for 3.1 [1.4.2, 1.4.4].

The 12-month change-frequency block in the generated section is all zeros because the
repository's last commit is 2024-05-01, more than 12 months before the audit date. Full
history was used instead.

## Build logic (verified)

- `buildSrc/`: **absent**
- `includeBuild("build-logic")` in `settings.gradle.kts` — composite build present
- `build-logic/settings.gradle.kts` includes exactly **one** subproject, `:convention`
- 4 convention plugins live in that single subproject: `application`, `library`, `model`, `signing`
- version catalog: single `gradle/libs.versions.toml`; no `Dependencies.kt`
- convention plugins use the **legacy AGP DSL** (`com.android.build.gradle.AppExtension`,
  `LibraryExtension`, `compileSdkVersion = "android-34"` as a string), consistent with the
  7.3.1 API they compile against
- `AndroidApplicationPlugin.kt:11` carries a stray IDE auto-import
  (`org.jetbrains.kotlin.de.undercouch.gradle.tasks.download...LogFactory.release`) — harmless,
  but evidence the file was not reviewed after generation

Every module declares its plugins twice: `alias(libs.plugins.android.library)` +
`alias(libs.plugins.kotlin.android)` + Hilt + KSP in its own `plugins {}` block, and then
`id("pl.smcebi.recipeme.library")`, which applies `com.android.library` and
`org.jetbrains.kotlin.android` again. The convention plugin is not actually centralising
plugin application.

## Hidden inputs (verified)

- `buildConfig = true` in `:app` (via `AndroidApplicationPlugin`) and in `:retrofit-base`,
  `:retrofit-deepl`, `:retrofit-openfoodfacts`
- **No** git-hash or timestamp `BuildConfig` fields. The version name is computed from four
  static catalog entries (`VersionNameResolver.kt`), which is deterministic.
- `gradleLocalProperties(rootDir)` is called at **configuration time** in
  `retrofit-base/build.gradle.kts:47`, `retrofit-deepl/build.gradle.kts:48` and
  `retrofit-openfoodfacts/build.gradle.kts` to feed `buildConfigField`
- `SigningPlugin.kt:13-17` opens `signing/release.properties` with `FileInputStream` at
  configuration time, unguarded by any `ValueSource` or provider
- root `build.gradle.kts:31` uses eager `tasks.create<Delete>("clean")`
- codegen on the critical path: KSP (Hilt + Dagger + Room), navigation-safeargs,
  kotlin-parcelize, kotlinx-serialization
- dynamic versions: **none**; no `gradle.lockfile`; no `verification-metadata.xml`

## Configuration surface (verified)

```properties
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true
org.gradle.caching=true
org.gradle.parallel=true
org.gradle.daemon=true
org.gradle.unsafe.configuration-cache=true
```

`org.gradle.unsafe.configuration-cache` is the **correct** property name for Gradle 8.0.2 —
the stable `org.gradle.configuration-cache` spelling arrived in 8.1. The audit script's
"absent" for `org.gradle.configuration-cache` is a version artefact, not a missing flag.
Configuration cache is on. Build cache is on. Parallel is on.

Absent and genuinely absent: `org.gradle.workers.max`, `kotlin.daemon.jvmargs`,
`org.gradle.isolated-projects` (does not exist in Gradle 8.0.2).

## Annotation processing (verified)

**kapt: 0 modules. KSP: 18 modules.** The migration is already done. `kotlin-kapt` survives
only as an unused alias in `libs.versions.toml`. Hilt aggregation is already enabled
(`hilt { enableAggregatingTask = true }`, `app/build.gradle.kts:20`).

The single most-quotable intervention named in the audit matrix (1.2.2 → 3.2.3) is therefore
**not available on this project**. Chapter 3 must not manufacture a regression to kapt in
order to produce a number.

## Shrinking (verified)

`isMinifyEnabled = true` in `release` only; `isShrinkResources = false`;
`minifyEnabled(false)` for the library `release` type. Debug is clean — there is no
accidental minification finding here. `android.enableR8.fullMode` is absent, and under
AGP 8.1.1 full mode is the default, so this is correct-by-default rather than a gap.

## Build-scan exposure — proprietary-code risk

`settings.gradle.kts:11` applies `com.gradle.enterprise` 3.12 and root `build.gradle.kts:35-38`
sets `termsOfServiceAgree = "yes"`. There is **no** `publishAlways()`, so nothing is uploaded
unless `--scan` is passed. The disabled CI workflow still contains the line
`run: ./gradlew assembleDebug --scan` (`.github/workflows/android.yml:60`, commented out).
Re-enabling that workflow as written would publish a scan of proprietary code to a public
`gradle.com` URL.

**No measurement in this study may use `--scan` on this project — `--profile` only.**

## CI (verified)

`.github/workflows/android.yml` exists but every line is commented out, headed
`## TEMPORARILY DISABLED`. There is no active CI. The commented content targets JDK 11.0.12
against a project whose toolchain is 17, uses `gradle/gradle-build-action@v2.4.0` for remote
caching, and writes secrets into `local.properties` before building. For 3.4.2 this is a
useful "what a real project's CI looks like before it is tuned" exhibit, but it produces no
measurements.

## Not measured / limitations (accumulating for 2.6)

1. `./gradlew :app:tasks --all` cross-check of the variant count — blocked by
   `Unable to establish loopback connection` in this environment. The static evidence is
   exhaustive; run the command locally to close it.
2. No task-time profile exists yet. Every `--profile` figure for 3.1 is outstanding.
3. No timings of any kind were taken in this session; the Docker image was not built.
4. Change frequency is measured over the full history (14 months of activity, dormant since
   2024-05-01), not a rolling 12-month window. The project is not under active development,
   so change frequency here is historical, not predictive.
5. The repository has no meaningful test suite (three `ExampleUnitTest.kt` stubs), so no
   `test`-task scenarios are available.
6. `--scan` is unavailable by policy, so scan-only data — task-input comparison, artifact
   transform listing, remote cache hit reporting — cannot be gathered for this project. That
   data has to come from Signal.
7. `:app:tasks --all` would also have given the registered task count for 1.3.1. Outstanding
   for the same reason.

## Findings — ranked against chapter 1

Effort is wall-clock estimate for the change itself, excluding measurement.
"Isolated?" asks whether the intervention can be measured on its own, with nothing else
moving — the requirement for it to earn a row in 3.3 rather than a paragraph in 3.2.

### Actionable, ranked

| # | observation | thesis | effort | isolated? |
|---|---|---|---|---|
| 1 | `gradleLocalProperties(rootDir)` is read during configuration in `retrofit-base`, `retrofit-deepl`, `retrofit-openfoodfacts`; `SigningPlugin.kt` opens `signing/release.properties` with a raw `FileInputStream`. Both are unrecorded configuration-time file inputs. | 1.3.1, 1.5.3 → 3.2.1 | 1–2 h (`providers.gradleProperty` / `ValueSource`) | **Yes.** Configuration-phase time and configuration-cache reuse rate, `help` task, nothing else changed. Highest-value item: a real defect in the author's own code, found by analysis, fixed and measured. |
| 2 | `org.gradle.workers.max` unset, `org.gradle.jvmargs=-Xmx2048m` for 26 projects with Hilt aggregation, `kotlin.daemon.jvmargs` unset. | 1.3.4, 1.6.1 → 3.2.1 | ~15 min per sweep point | **Yes**, and the effect should be *asymmetric* between a warm workstation and a CPU-capped container — this is the most direct available test of the thesis statement. |
| 3 | No `buildCache { }` block at all; caching is on but purely local-default. No remote cache. | 1.3.3 → 3.2.1, 3.4.2 | ~1 h for an explicit local dir cache; more for a remote node | **Yes.** The pure-CI intervention: near-zero on a warm machine, potentially the largest single effect in an ephemeral container. Cleanest asymmetry evidence in the study. |
| 4 | `build-logic` is a composite build with exactly **one** subproject, `:convention`, holding all four plugins. Touching any one plugin invalidates all consumers of all four. | 1.5.2 → 3.2.2 | 2–4 h to split into `:plugins` / `:tools`-style subprojects | **Yes**, via the `touch_build_logic` scenario. Signal is the live counter-example and gives the passage its comparison. |
| 5 | Root `build.gradle.kts` declares `allprojects { repositories { … } }` — cross-project configuration. | 1.3.1 → 3.2.2 | ~1 h (move to `dependencyResolutionManagement`, `FAIL_ON_PROJECT_REPOS`) | Partly. Small direct effect expected; its real role is as a prerequisite for Isolated Projects, which needs finding 7 first. |
| 6 | Every module applies `com.android.library` + `org.jetbrains.kotlin.android` twice — once by alias in its own `plugins {}` block and again inside `pl.smcebi.recipeme.library`. | 1.5.2 → 3.2.2 | ~2 h across 24 modules | Yes, but predict noise-level. Report the null honestly if that is what comes out. |
| 7 | Gradle 8.0.2 / AGP 8.1.1 / Kotlin 1.8.22 (K1). Current reference points are Gradle 9.x / AGP 9.x / K2. | 1.3.3, 1.3.4 → 3.2.3 | days, with real regression risk | **No.** An upgrade moves the compiler, the task implementations and the defaults simultaneously. Measure it as a stated before/after of the whole toolchain, never as a single-technique row in 3.3. |
| 8 | Three version skews: `build-logic` compiles against AGP **7.3.1** while the build applies **8.1.1**; KSP **1.9.0**-1.0.12 on Kotlin **1.8.22**; serialization plugin 1.8.20 vs Kotlin 1.8.22. | 1.5.1, 1.5.4 → 3.2.3 | ~2 h | **No** — this is correctness hygiene, not an optimisation. It is a *prerequisite*: measuring on a skewed toolchain weakens every downstream result. Do it first, do not claim a time saving for it. |
| 9 | Eager `tasks.create<Delete>("clean")` in the root build script. | 1.3.1 | 5 min | Technically yes; predict an unmeasurable effect at this project size. Worth one sentence as a confirmed-negligible finding. |
| 10 | No dependency locking, no `verification-metadata.xml`. | 1.5.4 | ~1 h | Only indirectly, via resolution cost with and without `--offline`. Reproducibility argument is stronger than the timing argument. |
| 11 | CI workflow exists but is 100 % commented out; when it ran it used JDK 11 against a JDK-17 toolchain and `assembleDebug --scan`. | 3.4.2 | n/a | **No** — nothing to measure. Material for the CI checklist and for the build-scan warning, not for 3.3. |

### Confirmed negatives — checked, absent

Each of these is a **result**, not a gap. A negative finding on the author's own code is
stronger evidence of honest analysis than a manufactured positive.

| checked for | thesis | verdict |
|---|---|---|
| kapt anywhere | 1.2.2 → 3.2.3 | **0 modules.** KSP in 18. Migration already done. The flagship intervention of the audit matrix is unavailable here. Do not manufacture a regression to kapt to produce a number. |
| `buildSrc` present, or `build-logic` undivided | 1.5.2 | `buildSrc` absent — composite build already in place. But `build-logic` **is** undivided; see actionable finding 4. |
| configuration cache / build cache / parallel missing | 1.3.3 | All three present and on. `org.gradle.unsafe.configuration-cache` is the **correct** spelling for Gradle 8.0.2 — renamed in 8.1 — so the audit script's "absent" is a version artefact. Whether the configuration cache actually *reuses* is a separate question, and finding 1 is the thing most likely to be breaking it. |
| `api(project(...))` without justification | 1.4.2 | **0 occurrences.** All 66 inter-module edges are `implementation`. Compile avoidance is already maximal at the wiring level. No `api`→`implementation` intervention exists here; an ABI-propagation experiment would have to be *induced* and labelled as a controlled manipulation. |
| `minifyEnabled` in a debug-like build type | 1.2.4 | **No.** `isMinifyEnabled = true` in `release` only, `false` for library `release`, debug clean. |
| `BuildConfig` fields from git or timestamp | 1.5.3 | **None.** Version name comes from four static catalog entries via `VersionNameResolver.kt` — deterministic. `BuildConfig` fields do exist in the three `retrofit-*` modules, but they come from `local.properties`, which is finding 1. |
| work executed during the configuration phase | 1.3.1 | **Present** — see finding 1. The audit script missed it because its grep looks for `providers.exec`/`ProcessBuilder`/`git`, and this project reads a properties file instead of forking a process. |
| dynamic versions | 1.5.4 | **None.** Every coordinate is pinned through the version catalog. |
| variant explosion | 1.2.5, 1.3.1, 1.4.3 | **2 variants.** No flavor dimensions, no product flavors. The phenomenon is demonstrable only on Signal (48). |

### ABI / non-ABI targets — three graph positions

All three paths verified to exist at `thesis-baseline`, and all three files end with a
closing brace of a top-level class body, which is what gradle-profiler's Kotlin mutators
need in order to inject a public (ABI) or private (non-ABI) member.

| position | module | consumers | commits | target file | why |
|---|---|---|---|---|---|
| leaf | `:ui-home` | 1 (`:app`) | 40 | `ui-home/src/main/java/pl/smcebi/recipeme/ui/home/main/HomeViewModel.kt` | Largest module in the project (1 110 LOC, 19.5 % of all Kotlin) and the most-edited library module, yet only one module sits downstream. That combination — maximum own compile cost, minimum blast radius — is what makes it the control arm: ABI and non-ABI should converge. `HomeViewModel` is `internal`, which reinforces the point rather than weakening it: its signature is invisible outside the module by construction. |
| mid-graph | `:domain-common` | 6 | 14 | `domain-common/src/main/java/pl/smcebi/recipeme/domain/common/utils/DomainResult.kt` | The only module with substantial fan-in *and* fan-out (6 consumers, 5 dependencies), i.e. genuinely mid-stack between the datasource and ui layers. Severity 84, second in the project. `DomainResult` is the sealed type that actually crosses the boundary into every domain and ui consumer, so a signature change there is an ABI change consumers can see. |
| max blast radius | `:datasource-common` | **9 (max)** | 2 | `datasource-common/src/main/java/pl/smcebi/recipeme/datasource/common/NetworkResult.kt` | Largest fan-in in the graph by a clear margin (next is 6). `NetworkResult` is its central public type and the one every consumer imports. Note for 3.1 that its severity is only 18 — nine dependents, two commits in the entire history. It is the *harmless* god module of 1.4.4, while the severity-weighted one is `:ui-common` (4 × 31 = 124). That the two metrics point at different modules is itself a finding. |
